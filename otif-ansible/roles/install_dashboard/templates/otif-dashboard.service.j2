[Unit]
Description=InfoFusion Dashboard
After=network.target

[Service]
Type=forking
ExecStart={{dashboard_target_dir}}/bin/catalina.sh start
ExecStop={{dashboard_target_dir}}/bin/catalina.sh stop

[Install]
WantedBy=multi-user.target
