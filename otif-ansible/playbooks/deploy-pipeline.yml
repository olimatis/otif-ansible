---

- hosts: pipeline, zookeeper, kafka, mongodb, text-mining

  tasks:

  - name: Depploy InfoFusion Ingestion Pipeline
    include_role:
      name: install_pipeline
    tags: 'install-pipeline'
    when: inventory_hostname in groups['pipeline']

